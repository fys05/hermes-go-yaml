apiVersion: v1
kind: Service
metadata:
  name: yaml-validator
  namespace: ${K8S_NAMESPACE}
  labels:
    app: yaml-validator
spec:
  type: NodePort
  selector:
    app: yaml-validator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
