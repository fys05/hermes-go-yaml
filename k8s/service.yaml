apiVersion: v1
kind: Service
metadata:
  name: yaml-validator
  namespace: ${K8S_NAMESPACE}
  labels:
    app: yaml-validator
spec:
  type: ClusterIP
  selector:
    app: yaml-validator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
